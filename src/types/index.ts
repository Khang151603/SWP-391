// Nơi khai báo các type/interface dùng chung toàn dự án

export type ID = string | number;

// Re-export User type from API types for convenience
export type { User } from '../api/types/user.types';


